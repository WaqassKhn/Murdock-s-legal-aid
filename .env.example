# Copy to .env. Never commit .env or document data.
LEGALLENS_DATABASE_URL=sqlite:///./data/legallens.db
LEGALLENS_STORAGE_DIR=./data/files
LEGALLENS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:8000
LEGALLENS_SECURE_COOKIES=false
LEGALLENS_AUTO_MIGRATE=true
LEGALLENS_MAX_UPLOAD_MB=20
LEGALLENS_MAX_WORKSPACE_DOCUMENTS=50
LEGALLENS_EXPENSIVE_REQUESTS_PER_MINUTE=20

# Required only for Docker Compose. Set a unique password before starting it.
POSTGRES_PASSWORD=

# GenAI: set all three values to enable generated analysis, answers, comparisons and lawyer questions.
# Empty values select explicitly labeled local extraction.
# Gemini example base URL: https://generativelanguage.googleapis.com/v1beta/openai
# Tested model: gemini-3.5-flash-lite (availability is account-dependent).
# Data is sent to the configured provider only when enabled. Review its retention terms.
LEGALLENS_MODEL_BASE_URL=
LEGALLENS_MODEL_API_KEY=
LEGALLENS_MODEL_NAME=
# Optional learned dense embeddings. BASE_URL and API_KEY may fall back to MODEL settings.
LEGALLENS_EMBEDDING_BASE_URL=
LEGALLENS_EMBEDDING_API_KEY=
LEGALLENS_EMBEDDING_MODEL=

# Optional persistent private storage (required by api/index.py on Vercel).
LEGALLENS_SUPABASE_URL=
LEGALLENS_SUPABASE_SECRET_KEY=
LEGALLENS_SUPABASE_BUCKET=legallens-documents
# The Vercel entrypoint enables serverless mode, secure cookies and 3 MB / 30-page limits.
# Keep false for the existing single-process Docker deployment.
LEGALLENS_SERVERLESS=false
